// Generated by CoffeeScript 2.3.1
(function() {
  /*

  parser = new NEARLEY.Parser NEARLEY.Grammar.fromCompiled G
  debug join ( key for key of parser ), ' '
  debug parser.options
   * parser.rewind() is deprecated https://github.com/kach/nearley/issues/261

   */
  'use strict';
  var CND, Randx, Xre, _, alert, badge, debug, echo, eq, help, i, info, join, jr, log, pattern, randexp, rpr, test, urge, warn, whisper, x;

  //###########################################################################################################
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'XXX/TESTS';

  log = CND.get_logger('plain', badge);

  info = CND.get_logger('info', badge);

  whisper = CND.get_logger('whisper', badge);

  alert = CND.get_logger('alert', badge);

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  echo = CND.echo.bind(CND);

  //...........................................................................................................
  test = require('guy-test');

  eq = CND.equals;

  jr = JSON.stringify;

  //...........................................................................................................
  // SQLISH                    = require '../sqlish'
  join = function(x, joiner = '') {
    return x.join(joiner);
  };

  Xre = require('xregexp');

  // Xre.install 'astral'
  Randx = require('randexp');

  // debug x = Xre '\\p{Sc}\\pN{3}'
  // x = Xre '(?A)^\\pL{1,10}$'
  x = Xre('\\pL{1,10}');

  // x = Xre '(?A)\\pL\\pN*'
  x = Xre('[_\\pL][-_\\pL\\pN]{0,30}');

  pattern = new RegExp(x.source);

  // pattern = /...[äöüabcde]+/
  randexp = new Randx(pattern);

  randexp.defaultRange.subtract(0x20, 0x7f);

  randexp.defaultRange.add(0x20, 0x02af);

// randexp.defaultRange.add 0x0000, 0xffff
// randexp.defaultRange.add 0x20000, 0x2000f
  for (_ = i = 1; i <= 10; _ = ++i) {
    debug(randexp.gen());
  }

  //-----------------------------------------------------------------------------------------------------------

  //###########################################################################################################
  if (module.parent == null) {
    null;
  }

}).call(this);

//# sourceMappingURL=generator.js.map
